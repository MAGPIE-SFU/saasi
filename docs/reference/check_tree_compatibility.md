# Check if tree is compatible with saasi

Validates tree structure without modifying it.

## Usage

``` r
check_tree_compatibility(tree)
```

## Arguments

- tree:

  A phylo object

## Value

Logical. TRUE if tree is compatible with saasi
