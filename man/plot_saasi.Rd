% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_saasi.R
\name{plot_saasi}
\alias{plot_saasi}
\title{Plot phylogenetic tree with estimated ancestral states from SAASI}
\usage{
plot_saasi(
  tree,
  saasi_result,
  colors = NULL,
  tip_cex = 0.5,
  node_cex = 0.2,
  save_file = NULL,
  width = 3000,
  height = 3000,
  res = 300
)
}
\arguments{
\item{tree}{A phylogenetic tree of class \code{phylo} prepared using \code{prepare_tree_for_saasi}. This is the same tree as in the \code{saasi} input.}

\item{saasi_result}{Output from \code{saasi}.}

\item{colors}{Character vector of colors. If NULL, colors are automatically
generated based on the number of states (default: NULL)}

\item{tip_cex}{Size of tip circles (default: 0.5)}

\item{node_cex}{Size of node pie charts (default: 0.2)}

\item{save_file}{Character. File path to save the plot (e.g. "tree.png").
If NULL, plot is drawn to current device (default: NULL)}

\item{width}{Plot width in pixels when saving (default: 3000)}

\item{height}{Plot height in pixels when saving (default: 3000)}

\item{res}{Plot resolution in dpi when saving (default: 300)}
}
\value{
Invisibly returns the color vector used
}
\description{
Plot phylogenetic tree with estimated ancestral states from SAASI
}
\examples{
# Basic plot
plot_saasi(tree, saasi_result)

# Custom colors and save to file
plot_saasi(tree, saasi_result,
           colors = c("red", "blue", "green"),
           node_cex = 0.3,
           save_file = "tree.png")

}
